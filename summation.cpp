#include <iostream>
using namespace std;

int sum() {
    // inputs
    cout << "starting value (must be larger than 0): ";
    cin >> ii; // index input
    cout << "upper limit: ";
    cin >> upperLimit;
    cout << "equation (must use 'ii'): ";
    cin >> equation;

    // functionality
    for (int upperLimit < upperLimit + 1; ii++) {
        output = equation
        cout << "=======\n" << output "=======\n"
    }
}
